# Memory

Memory cards are manually appended to `memory/cards.jsonl`. Later records supersede earlier records with the same ID. Repository match is mandatory; task, file, risk, and priority scores use fixed integer weights. At most ten cards and 4,000 characters are injected. Review and finish never write memory.

